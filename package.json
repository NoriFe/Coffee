{
  "devDependencies": {
    "parcel": "^2.10.2",
    "postcss": "^8.4.31",
    "tailwindcss": "^3.3.5"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel src/*.html",
    "build": "parcel build src/*.html"
  },
  "dependencies": {
    "sharp": "^0.31.1"
  }
}
